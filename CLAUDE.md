# railway-projects

If the user hands you an HTML file and asks for it to be deployed, follow
**"The standard request"** in [AGENTS.md](AGENTS.md) — that is this repo's
main workflow.

See **[AGENTS.md](AGENTS.md)** for everything else — layout, how to create and
write a page, the server contract, routing and domains, secrets handling, and
branching (main only).

`AGENTS.md` is the single source of truth. Don't duplicate its rules here.
