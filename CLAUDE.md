# railway-projects

See **[AGENTS.md](AGENTS.md)** for all repository instructions — layout, how to
create and write a page, the server contract, how each page is deployed as its
own Railway service, secrets handling, and branching.

`AGENTS.md` is the single source of truth. Don't duplicate its rules here.
